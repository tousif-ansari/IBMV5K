---
description: "Use the lsarrayrecommendation command to view a recommended configuration for the specified drive class and number of drives."
enabled: true
runner_type: "python-script"
entry_point: "lsarrayrecommendation.py"
name: "lsarrayrecommendatio"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  className:
    type: string
    description: "name of the array."
    required: true







