---
description: "Use the analyzevdisk command to queue or cancel volume analysis"
enabled: true
runner_type: "python-script"
entry_point: "analyzevdisk.py"
name: "analyzevdisk"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  disk:
    type: integer
    description: "name of the disk."
    required: true







