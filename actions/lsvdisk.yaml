---
description: "Use the lsvdisk command to display a concise list or a detailed view of volumes that are recognized by the clustered system"
enabled: true
runner_type: "python-script"
entry_point: "lsvdisk.py"
name: "lsvdisk"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  delim:
    type: string
    description: "name of the delim."
    required: true







