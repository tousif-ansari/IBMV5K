---
description: "Use the lsvdiskdependentmaps command to display all FlashCopy® mappings with target volumes that are dependent upon data held on the specified volume.
"
enabled: true
runner_type: "python-script"
entry_point: "lsvdiskdependentmaps.py"
name: "lsvdiskdependentmaps"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  delim:
    type: string
    description: "put the delim"
    required: true








