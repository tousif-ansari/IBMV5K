---
description: "Use the lsvdisklba command to list the volume and logical block address (LBA) for the specified storage pool LBA"
enabled: true
runner_type: "python-script"
entry_point: "lsvdisklba.py"
name: "lsvdisklba"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  mdisk:
    type: integer
    description: "put the diskname"
    required: true
  mdisklba:
    type: integer
    description: "put the disklba"
    required: true








