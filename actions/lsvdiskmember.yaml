---
description: "Use the lsvdiskmember command to display a list of MDisks that are members of the specified volume."
enabled: true
runner_type: "python-script"
entry_point: "lsvdiskmember.py"
name: "lsvdiskmember"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  member:
    type: integer
    description: "put the membername"
    required: true








