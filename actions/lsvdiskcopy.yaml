---
description: "Use the lsvdiskcopy command to list volume copy information."
enabled: true
runner_type: "python-script"
entry_point: "lsvdiskcopy.py"
name: "lsvdiskcopy"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  delim:
    type: string
    description: "put the delim"
    required: true








