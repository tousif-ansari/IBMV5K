---
description: "Use the lssevdiskcopy command to list the thin-provisioned copies of the specified volumes."
enabled: true
runner_type: "python-script"
entry_point: "lssevdiskcopy.py"
name: "lssevdiskcopy"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  delim:
    type: string
    description: "name of the delim."
    required: true







