---
description: "Use the lsvdiskfcmapcopies command to display a list of all FlashCopy® mappings with a target volume containing a valid copy of the specified volume."
enabled: true
runner_type: "python-script"
entry_point: "lsvdiskfcmapcopies.py"
name: "lsvdiskfcmapcopies"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  delim:
    type: string
    description: "put the delim"
    required: true








