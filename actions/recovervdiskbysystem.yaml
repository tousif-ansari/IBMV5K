---
description: "Use the recovervdiskbysystem command to acknowledge data loss for all volumes in the clustered system (system) with a fast_write_state of corrupt and bring the volumes back online."
enabled: true
runner_type: "python-script"
entry_point: "recovervdiskbysystem.py"
name: "recovervdiskbysystem"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true




