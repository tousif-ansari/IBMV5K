---
description: "Use the lsmetadatavdisk command to display the information for metadata volume."
enabled: true
runner_type: "python-script"
entry_point: "lsmetadatavdisk.py"
name: "lsmetadatavdisk"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true








