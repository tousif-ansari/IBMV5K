---
description: "Use the lsarraymember command to list the member drives of one or more array MDisks."
enabled: true
runner_type: "python-script"
entry_point: "lsarraymember.py"
name: "lsarraymember"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  delimName:
    type: string
    description: "name of the array."
    required: true







