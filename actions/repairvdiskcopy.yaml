---
description: "Use the repairvdiskcopy command to detect and (optionally) correct any volume copies that are not identical."
enabled: true
runner_type: "python-script"
entry_point: "repairvdiskcopy.py"
name: "repairvdiskcopy"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  startlba:
    type: string
    description: "put the startlba"
    required: true




