---
description: "Use the lsvdiskaccess command to display a list of all input/output (I/O) groups in the volume access set."
enabled: true
runner_type: "python-script"
entry_point: "lsvdiskaccess.py"
name: "lsvdiskaccess"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  disk:
    type: integer
    description: "name of the delim."
    required: true







