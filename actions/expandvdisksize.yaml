---
description: "Use the expandvdisksize command to expand the size of a volume by a given capacity."
enabled: true
runner_type: "python-script"
entry_point: "expandvdisksize.py"
name: "expandvdisksize"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  size:
    type: integer
    description: "put the size."
    required: true
  unit:
    type: string
    description: "put the unit."
    required: true
  mdisk:
    type: string
    description: "put the mdisk."
    required: true
  fmtdisk:
    type: string
    description: "put the fmtdisk."
    required: true






