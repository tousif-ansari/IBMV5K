---
description: "host_disk_map"
enabled: true
runner_type: "python-script"
entry_point: "mkvdiskhostmap.py"
name: "mkvdiskhostmap"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  host:
    type: string
    description: "put the host"
    required: true
  scsi:
    type: integer
    description: "put the scsi"
    required: true
 



