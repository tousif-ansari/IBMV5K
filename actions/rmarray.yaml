---
description: "Use the rmarray command to remove an array MDisk from the configuration."
enabled: true
runner_type: "python-script"
entry_point: "rmarray.py"
name: "lsarray"
parameters:
  ip:
    type: string
    description: "IP address used when connecting to windows host"
    required: false
  username:
    type: string
    description: "username."
    required: false
  password:
    type: string
    description: "Optional override of the password in <credential_name>."
    required: false
    secret: true
  mdisk:
    type: string
    description: "name of the mdisk."
    required: true







